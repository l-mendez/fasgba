# Legacy SQL Archive

These files are historical SQL snippets from before the project standardized on
Supabase CLI migrations. They are kept only for context while auditing old
schema decisions.

Do not apply these files directly to Supabase. Create a new migration under
`supabase/migrations/` for any future database change.

Current canonical workflow: see `docs/database.md`.
